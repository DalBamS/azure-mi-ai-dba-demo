# Demo Cockpit local configuration template.
# Keep real secrets in your local .env only; do not commit them.

# Live SQL execution remains opt-in.
COCKPIT_MODE=mock
COCKPIT_ALLOW_LIVE=0

# Self-hosted SLM inside the data boundary (Ollama OpenAI-compatible API).
SLM_ENDPOINT=http://127.0.0.1:11434/v1
SLM_MODEL=qwen2.5:3b
# SLM_API_KEY=
