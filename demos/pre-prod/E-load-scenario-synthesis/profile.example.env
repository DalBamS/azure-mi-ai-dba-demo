# ---------------------------------------------------------------------------
# Synthesized load profile (SYNTHETIC data / no PII) - generated by demo E.
#   request : 런칭 첫날 동접 5만, 재화 40%/랭킹 30%
#   CCU     : 50,000  (active_ratio=0.02)
#   mix     : currency 40% / ranking 30% / inventory 30%
# game-driver: copy into repo-root .env, then `python workload/game-driver/driver.py`
# ---------------------------------------------------------------------------
WORKLOAD_CONCURRENCY=512
WORKLOAD_DURATION_SECONDS=600
WORKLOAD_MIX_CURRENCY_TRANSFER=40
WORKLOAD_MIX_RANKING_QUERY=30
WORKLOAD_MIX_INVENTORY_UPDATE=30

# HammerDB TPROC-C baseline (separate DB) - set in build_tproc.tcl / run_tproc.tcl:
#   diset tpcc mssqls_count_ware 20
#   diset tpcc mssqls_num_vu     128
